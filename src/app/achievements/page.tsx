import Layout from '../../components/Layout';

const Achievements: React.FC = () => {
    return (
        <Layout>
            Achievements
        </Layout>
    );
};

export default Achievements;
